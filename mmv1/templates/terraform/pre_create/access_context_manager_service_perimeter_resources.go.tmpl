updateMask := []string{"status.resources", "etag"}

// updateMask is a URL parameter but not present in the schema, so ReplaceVars
// won't set it
url, err = transport_tpg.AddQueryParams(url, map[string]string{"updateMask": strings.Join(updateMask, ",")})
if err != nil {
	return err
}

resources_to_add := d.Get("resources").(*schema.Set).List()

etag, resources, err := AccessContextManagerServicePerimeterResourcesBuildResourcesUpdateRequest(d, nil, resources_to_add, meta)

if err != nil {
	return err
}

obj["status"] = make(map[string]interface{})
obj["status"].(map[string]interface{})["resources"] = resources
obj["etag"] = etag

delete(obj, "resources")
